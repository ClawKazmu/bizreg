# Optional: database URL (if using PostgreSQL)
DATABASE_URL=postgresql://user:pass@localhost/dbname

# Scraping settings (rate limits)
SCRAPER_DELAY=1

# Rate limiting configuration
FREE_TIER_LIMIT=20
RATE_LIMIT_DB=data/rate_limits.json

# Logging
LOG_LEVEL=INFO